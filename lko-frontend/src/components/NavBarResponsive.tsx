import React from 'react';
import NavBar from './NavBar';

const NavBarResponsive: React.FC = () => (
  <div>
    {/* Placeholder for responsive navigation (e.g., hamburger menu) */}
    <NavBar />
  </div>
);

export default NavBarResponsive;
